# reference/

`talent-xray.html` — the working single-file console with the tested query composer —
goes in this directory. It is placed by Chris, not generated.

**Read it, port its logic verbatim, never modify or reformat it.** It is the only part
of the system validated against live results. (It is excluded in `.prettierignore` for
this reason.)

Status: not yet placed. W2 cannot start until it is.
